version: v1.0
name: Gradle Build and Test
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Gradle Build
    task:
      jobs:
        - name: build
          commands:
            - checkout
            - ./gradlew build
            - cache store
  - name: Gradle Test
    task:
      jobs:
        - name: test
          commands:
            - checkout
            - cache restore
            - ./gradlew test
            - cache store
promotions:
  - name: Promote to integration test
    pipeline_file: integration_test.yml
    auto_promote:
      when: branch = 'master' AND result = 'passed'
