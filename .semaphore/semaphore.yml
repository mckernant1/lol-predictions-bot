version: v1.0
name: Gradle Build and Test
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Gradle Build
    task:
      jobs:
        - name: build
          commands:
            - checkout
            - ./gradlew build
  - name: Gradle Test
    task:
      jobs:
        - name: test
          commands:
            - checkout
            - ./gradlew test
promotions:
  - name: Dockerize
    pipeline_file: pipeline_2.yml
    auto_promote:
      when: branch = 'master' AND result = 'passed'
